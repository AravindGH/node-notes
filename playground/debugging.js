var person= {

    name: 'Aravind'
};

person.age = 25;

//we can use the debugger tool by adding the debugger;
//we dont need to use the n till this step;
debugger;

//the best way to debug is the dev chrome 
// with the help of it, we would be able to test it --> really helpful tool!!!


person.name = 'Mike';

console.log(person);
